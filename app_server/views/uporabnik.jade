extends layout.jade

block content
    #uporabnik
        h1 to je stran za novice
        h2 tukaj se bodo nahajale stvari za novice
        p  pa se en paragraf za dobro mero
        h1 TITLE: #{title}
        h2 GLAVA: #{glava}
        h3 TIC: #{tic}

